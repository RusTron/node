import React from 'react';
import './Header.scss';

export const Header = () => (
  <header className="App-header">
    <span
      className="App-title"
    >
      Weather Forecast
    </span>
  </header>
);
